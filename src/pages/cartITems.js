import React, { useContext } from 'react'
import CartContext from '../context/cartContext'

const cartITems = () => {
    const things = useContextThigsContext
    
    return(
        <ul>
            
        </ul>
    )
}

export default cartITems